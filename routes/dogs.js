const db = require('../models/dog');

const DogtRoutes = require('./routes/dogs');
    app.get('/dogs', );
    app.post('/dogs', );
    app.get('/dogs/:id', );
    app.put('/dogs/:id', );
    app.delete('/dogs/:id', );
